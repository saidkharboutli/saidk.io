---
import { CTA } from '@/partials/CTA';
import { Hero } from '@/partials/Hero';
import { ProjectList } from '@/partials/ProjectList';
import { RecentPosts } from '@/partials/RecentPosts';
import Base from '@/templates/Base.astro';
// eslint-disable-next-line prettier/prettier
import type { IFrontmatter } from '@/types/IFrontMatter';
import { AppConfig } from '@/utils/AppConfig';
import { sortByDate } from '@/utils/Posts';

const allPosts = await Astro.glob<IFrontmatter>('./blog/**/*.md');
const sortedPosts = sortByDate(allPosts);
const last4Posts = sortedPosts.slice(0, 4);

const { title } = AppConfig;
const { description } = AppConfig;
---

<Base head={{ title, description }}>
  <Hero />
  <RecentPosts postList={last4Posts} />
  <ProjectList />
  <CTA />
</Base>


<!--
  TODO:
  - Blog post <-> project relation
  - Series entries at bottom of blog post
  - Blog post comments (giscus)
  - Blog post views?
  - Projects view (modeled after blog posts)
  - Portfolio
  - Aboutme
-->